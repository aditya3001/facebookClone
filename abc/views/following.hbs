<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    {{!--
    <link rel="stylesheet" href="style.css">
    <link rel="stylesheet" href="homeStyles.css"> --}}
    <style>
        #homeHeader1 {
            width: 100%;
            height: 100px;
            /* margin: auto; */
            padding: 10px;
            background: #222 url('https://subtlepatterns.com/patterns/use_your_illusion.png') repeat;
            padding: 10px;
        }
        h1{
            color:white;
        }
    </style>
</head>

<body>
    <header id="homeHeader1">
        <h1>{{name}}</h1>

        <a style="color:aliceblue" href="/logout" id="logout">Logout</a>
    </header>

    <div id="myDiv">
        {{#each following as |follower|}}
        <div class="card">
            <a href="/profile/{{follower._id}}">{{follower.name}}</a>
        </div>
        {{/each}}
    </div>
</body>

</html>